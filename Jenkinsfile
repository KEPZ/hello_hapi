#!/usr/bin/env bash

pipeline {

    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                exit 0
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
                exit 0
            }
        }
    }
}
